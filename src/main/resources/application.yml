bootstrap:
  servers: localhost:9092
  retrise: 0
  batch:
    size: 4096
  linger:
    ms: 1
  buffer:
     memory: 40960
     
server:
  port: 8080
Spring:
  mvc:
    view:
      prefix: /WEB-INF/jsp/
      suffix: .jsp
      
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    jdbc-url: jdbc:mysql://localhost:3306/study_db?serverTimezone=UTC&allowPublicKeyRetrieval=true&useSSL=false
    username: study_user
    password: pass@word1